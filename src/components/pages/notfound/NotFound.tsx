import { FC } from 'react';
import styles from './NotFound.module.css';

const NotFound:FC = () => {
    return (
      <div className={`form-wrapper ${styles.ups}`}>
      </div>
    );
};

export default NotFound;